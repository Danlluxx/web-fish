# AquaMarket Catalog

Минималистичный fullstack-каталог на `Next.js + TypeScript` для ассортимента AquaMarket с избранным, корзиной и оформлением заказа без онлайн-оплаты и личного кабинета. Сейчас каталог импортирован из реального Excel-прайса и содержит `481` позицию.

## Стек

- `Next.js (App Router)` для SSR, SEO-страниц и единого frontend/backend-проекта.
- `TypeScript` для типизированной модели товаров, фильтров и API.
- `CSS` с собственной дизайн-системой вместо тяжелого UI-слоя.
- `Route Handlers` в `app/api/*` для JSON API каталога, поиска и карточек товаров.
- Генерация JSON-каталога из Excel через `python3`-скрипт без внешних Python-библиотек.

## Что реализовано

- Главная страница с позиционированием проекта и быстрым входом в каталог.
- Общий каталог `/catalog`.
- Страницы категорий `/catalog/[categorySlug]`.
- Страницы подкатегорий `/catalog/[categorySlug]/[subcategorySlug]`.
- Карточка товара `/products/[slug]`.
- Поиск по названию, категории и подкатегории.
- Фильтры по категории и подкатегории.
- Пагинация для большого каталога.
- Избранное `/favorites`.
- Корзина `/cart` с выбором количества.
- Оформление заказа через `POST /api/orders`.
- Отправка Excel-файла заказа на почту менеджера, если настроен SMTP.
- SEO-маршруты, `sitemap`, `robots`.
- Отдельный Excel-прайс в `public/files/AquaMarket-10-04.xlsx`.
- API:
  - `GET /api/products`
  - `GET /api/products/[slug]`
  - `GET /api/search`
  - `GET /api/product-image/[slug]`
  - `POST /api/orders`

## Архитектура

### 1. Слой данных

- `scripts/import_price_list.py`
  - читает `.xlsx` напрямую через стандартную библиотеку Python;
  - восстанавливает категории, подкатегории и товары из Excel-прайса;
  - генерирует `data/catalog.generated.json`;
  - копирует исходный Excel в `public/files/` для скачивания на сайте.

- `data/catalog.generated.json`
  - содержит уже подготовленные товары и структуру каталога;
  - обновляется повторным импортом нового Excel-файла.

- `lib/catalog/repository.ts`
  - интерфейс репозитория и in-memory реализация;
  - позволяет позже заменить источник данных на БД без переписывания UI.

- `lib/catalog/service.ts`
  - бизнес-логика каталога;
  - фильтрация, поиск, пагинация, похожие товары.

### 2. Frontend

- `app/*` отвечает за SSR-страницы и SEO.
- `components/catalog/*` содержит UI каталога, карточки, пагинацию, хлебные крошки и детальную страницу товара.
- `components/home/*` содержит стартовый экран.
- `app/globals.css` хранит общую дизайн-систему.

### 3. Backend и импорт

- Route handlers в `app/api/*` отдают JSON и могут использоваться:
  - для внешней интеграции;
  - для будущей клиентской автоподсказки;
  - для подключения админки или импорта.

## Рекомендация по базе данных для production

Для боевого каталога на `2000-3000` товаров лучшая практическая схема:

- `PostgreSQL` как основная БД.
- Таблицы:
  - `categories`
  - `subcategories`
  - `products`
  - при необходимости `product_images`
- Индексы:
  - `slug`
  - `category_id`
  - `subcategory_id`
  - полнотекстовый индекс по `title + description + category + subcategory`

Почему это оптимально:

- 2000-3000 товаров не требуют сложной поисковой инфраструктуры вроде Elasticsearch;
- PostgreSQL даст быстрый полнотекстовый поиск и простое сопровождение;
- структура легко расширяется под импорт из Excel/CSV или 1С.

## Как развивать дальше

### Подход для контента

- Хранить основной каталог в PostgreSQL.
- Импортировать товары пакетно из `Excel/CSV`.
- Изображения хранить в object storage/CDN.

### Подход для производительности

- Оставить SSR/SSG для SEO-страниц категорий и карточек товара.
- Добавить кеширование запросов к каталогу.
- Для больших коллекций можно заменить пагинацию на incremental loading.

### Подход для администрирования

- Добавить отдельную защищенную админ-панель.
- Подключить импорт прайса и выгрузок номенклатуры.
- Хранить SEO-мета и статус публикации у каждого товара.

## Импорт нового Excel

```bash
npm run import:catalog -- "/полный/путь/до/файла.xlsx"
```

По умолчанию команда:

- обновляет `data/catalog.generated.json`;
- копирует Excel в `public/files/AquaMarket-10-04.xlsx`.

## Запуск

```bash
npm install
npm run dev
```

## Почта заказов

После оформления заказа сервер:

- сохраняет заказ в `data/orders/orders.json`;
- формирует Excel-совместимый файл заказа;
- отправляет письмо с вложением на адрес менеджера, если заполнены SMTP-переменные.

Создайте `.env.local` по примеру `.env.example` и заполните:

- `SMTP_HOST`
- `SMTP_PORT`
- `SMTP_SECURE`
- `SMTP_USER`
- `SMTP_PASS`
- `SMTP_FROM_EMAIL`
- `SMTP_FROM_NAME`
- `ORDER_RECEIVER_EMAIL`

Если SMTP не настроен, заказ всё равно сохранится, а интерфейс покажет, что письмо не было отправлено.

## Примечание

Сейчас проект содержит демонстрационный набор товаров и готовую архитектуру под реальный каталог. Для production достаточно заменить in-memory репозиторий на источник данных из PostgreSQL и подключить реальную выгрузку изображений и Excel-прайса.
